{% extends 'MeowBundle::layout.html.twig' %}

{% block content %}


	<main class="cd-main-content" id="all-spoils">
		
		<h2>Tous les spoils</h2>

		<section class="cd-gallery">
			
			<ul>
				{% for spoil in spoils %}
					<li class="mix {{ spoil.manga.category.name|lower }}">
						<a href="{{ urlBase }}spoil/{{ spoil.slug }}">
							<figure>
								<div class="bloc-image">
									<img src="{{ asset('_assets/imgs/spoils/' ~ spoil.cover) }}" alt="{{ spoil.title }}">
								</div>
								<figcaption>
									<p class="titre-manga">{{ spoil.manga.name }}</p>
									<div class="infos-spoils">
										<p class="nb-spoil" data-icon="8">{{ spoil.nbSpoil }}</p>
										<p class="nb-commentaire" data-icon="x">{{ spoil.nbComments }}</p>
									</div>
									<div style="clear:both;"></div>
									<p class="spoil">{{ spoil.title }}</p>
									<p class="auteur">Par <span class="auteur-important">{{ spoil.author.username }}</span></p>
								</figcaption>
							</figure>
						</a>
					</li>

				{% endfor %}
				<li class="gap"></li>
				<li class="gap"></li>
				<li class="gap"></li>
			</ul>

			<div class="cd-fail-message">Aucun résultat</div>
		</section>
	 
		<div class="cd-filter">
			<form>
				<div class="cd-filter-block">
					<h4 style="background:url('{{ asset('_assets/imgs/cd-icon-arrow.svg') }}') no-repeat left center">Rechercher</h4>
					<div class="cd-filter-content">
						<input type="search" placeholder="Tapez votre manga">
					</div>
				</div>

				<div class="cd-filter-block">
					<h4 style="background:url('{{ asset('_assets/imgs/cd-icon-arrow.svg') }}') no-repeat left center">Type de manga</h4>
					<ul class="cd-filter-content cd-filters list">
						{% for category in categories %}
							<li>
								<input class="filter" data-filter=".{{ category.name|lower }}" type="checkbox" id="{{ category.name|lower }}">
								<label class="checkbox-label" for="{{ category.name|lower }}">
									{{ category.name|capitalize }}
								</label>
							</li>
						{% endfor %}
					</ul>
				</div>

			</form>
	 
			<a href="#0" class="cd-close" data-icon="v"></a>
		</div>
	 
		<a href="#0" class="cd-filter-trigger" style="background:url('{{ asset('_assets/imgs/cd-icon-filter.png') }}') no-repeat 10px center #e65354">Filtrer</a>
	</main>

	{# <table>
		<tr>
			<th>Id</th>
			<th>Lien</th>
			<th>Id / nom / catégorie manga</th>
			<th>Id / pseudo auteur</th>
			<th>Titre</th>
			<th>Slug</th>
			<th>Url image</th>
			<th>Date et heure</th>
			<th>Nombre de commentaires</th>
			<th>Nombre de spoils</th>
			<th>Nombre de fails</th>
			<th>Nombre de fakes</th>
			<th>Publié</th>
		</tr>
		{% for spoil in spoils %}
			<tr>
				<td>{{ spoil.id }}</td>
				<td><a href="{{ urlBase ~ "spoil/" ~ spoil.slug }}">lien</a></td>
				<td>{{ spoil.manga.id }} / {{ spoil.manga.name }} / {{ spoil.manga.category.name }}</td>
				<td>{{ spoil.author.id }} / {{ spoil.author.username }}</td>
				<td>{{ spoil.title }}</td>
				<td>{{ spoil.slug }}</td>
				<td>
					{% if spoil.cover is not empty %}
						{{ spoil.cover }}
					{% else %}
						placeholder.jpg
					{% endif %}
				</td>
				<td>{{ spoil.date|date("Y/m/d H:i:s") }}</td>
				<td>{{ spoil.nbComments }}</td>
				<td>{{ spoil.nbSpoil }}</td>
				<td>{{ spoil.nbFail }}</td>
				<td>{{ spoil.nbFake }}</td>
				<td>
					{% if spoil.isPublished == true %}
						Oui
					{% else %}
						Non
					{% endif %}
				</td>
			</tr>
		{% endfor %}
	</table> #}

	<script src="{{ asset('_assets/js/content-filter/jquery.mixitup.min.js') }}"></script>
	<script src="{{ asset('_assets/js/content-filter/main.js') }}"></script>
	<script src="{{ asset('_assets/js/content-filter/modernizr.js') }}"></script>
	

{% endblock %}